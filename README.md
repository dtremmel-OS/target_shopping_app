# Target Shopping Assistant V2

Features: multiple lists, favorites, recurring-style items, Target search, shopping mode foundation, prices, budgets, aisle/location, notes, duplicate lists, sharing, filters, local storage.

Install: host on HTTPS, open in Safari, Share → Add to Home Screen.

Target buttons use Target's public search URL and do not store Target credentials.